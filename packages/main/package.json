{
  "name": "main",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=development vite --force --host --open",
    "dev:prod": "cross-env NODE_ENV=production vite --mode production --force --host --open",
    "clean": "rm -rf build && rm -rf dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "antd": "4.21.6",
    "my": "workspace:*"
  }
}
