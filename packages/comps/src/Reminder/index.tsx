/* eslint-disable react-refresh/only-export-components */
import Reminder, { IReminderProps as _IReminderProps } from './Reminder'
import { useReminder } from './useReminder'
export type IReminderProps = _IReminderProps
export { Reminder, useReminder }
